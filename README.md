# YAGEC
Yet Another Game Engine in C++

---

The project structure is very simple for now:

`server` contains a 'smart' TypeScript server that runs the game logic

`client` contains a 'dumb' C++ client that colects input and renders to the screen

`shared` contains information (docs) about the contract between client and server
